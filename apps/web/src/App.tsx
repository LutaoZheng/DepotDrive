import { Navigate,Outlet,Route,Routes } from 'react-router-dom';import { useAuth } from './auth';import { AuthPage } from './pages/AuthPage';import { DrivePage } from './pages/DrivePage';
import { StorageDashboardPage } from './pages/StorageDashboardPage';
function Protected(){const {user,loading}=useAuth();if(loading)return <div className="grid min-h-screen place-items-center">Loading…</div>;return user?<Outlet/>:<Navigate to="/login" replace/>}function Guest({children}:{children:React.ReactNode}){const {user,loading}=useAuth();if(loading)return null;return user?<Navigate to="/drive" replace/>:children}
export default function App(){return <Routes><Route path="/login" element={<Guest><AuthPage mode="login"/></Guest>}/><Route path="/register" element={<Guest><AuthPage mode="register"/></Guest>}/><Route element={<Protected/>}><Route path="/drive" element={<DrivePage/>}/><Route path="/drive/folder/:folderId" element={<DrivePage/>}/><Route path="/storage" element={<StorageDashboardPage/>}/></Route><Route path="*" element={<Navigate to="/drive" replace/>}/></Routes>}
